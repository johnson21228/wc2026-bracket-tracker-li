# Lesson: Root Capture Hygiene in the Template

The registry dashboard build showed that root-level Capture Back files accumulate quickly during active work.

The reusable lesson is not dashboard-specific.

All Workbench repos should keep root clean and store Capture Back records under `captures/`.
