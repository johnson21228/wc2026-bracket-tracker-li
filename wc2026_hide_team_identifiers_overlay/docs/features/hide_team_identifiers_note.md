# Hide Team Identifiers Note

The public-facing site should be cleaner.

Hide visible metadata like:

```text
MEX - GA
USA - GD
```

The data still exists in JSON and can later be exposed in Steve's building controls if useful.
