# Game 1 Modal Chooser Note

The desired UI is not a complicated image overlay with visible control widgets.

The desired UI is:

```text
single background image
transparent R32 hit targets
external chooser window/frame
runtime pick storage
delete option
```
