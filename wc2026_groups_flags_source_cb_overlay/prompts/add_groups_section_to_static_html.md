# Prompt — Add Groups Section To Static HTML

```text
Update the WC2026 Bracket Tracker static HTML to include a Groups section.

Read LI first.

Requirements:
- Use group/team data from `data/groups_from_flags_images.json` or canonical `data/groups.json`.
- Show Groups A-L with flags and team names.
- Preserve canonical names and friendly display names.
- Make the section useful for Game 1 Round-of-32 picks.
- Preserve export/import JSON state.
- Produce a new static HTML release and WB_SESSION Capture Back.
```
