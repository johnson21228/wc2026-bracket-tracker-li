# Implemented Bracket Alignment Note

The static HTML now uses a clearer bracket tree layout:

```text
R32 pair → centered R16 slot
R16 pair → centered QF slot
QF pair  → centered SF slot
SF pair  → centered Final slot
```

This is intended to make screenshots easier to interpret later.
