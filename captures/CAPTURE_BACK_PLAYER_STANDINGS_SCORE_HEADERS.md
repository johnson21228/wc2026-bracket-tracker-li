# Capture Back: Player Standings Score Headers

## Outcome

Player Standings column headers now align with the scoring model.

## Change

- `Group` header becomes `Score`.
- `Knockout` header becomes `Max Possible`.

The existing runtime cell classes may remain temporarily for compatibility until the scoring runtime is implemented.
