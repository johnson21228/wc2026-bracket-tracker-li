# Capture Back — C64 Workbench Loop Easter Egg Tab

Added a third Workbench Easter egg panel tab: `C64 Loop`.

The tab explains how the Commodore 64 Learning Lab uses the Workbench loop:
conversation → repo change → verification → C64 build → emulator evidence → capture back → next lab.

The C64 Workbench image is stored in the repo under `site/assets/workflow/`.
