# Remove Tooltips And R32 Drop Only Note

The current page should be simpler.

For now:

```text
Only Round-of-32 slots are drop targets.
Later rounds are not drop targets.
Tooltips are removed.
```

Later-round progression should eventually be handled by winner selection or scoring/result logic, not arbitrary dragging.
