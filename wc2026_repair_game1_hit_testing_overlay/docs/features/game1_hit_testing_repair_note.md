# Game 1 Hit Testing Repair Note

The failure mode was likely caused by patching an already-mutated HTML page or opening a stale release page.

This repair rebuilds the canonical `game1_playfield.html` and creates a release copy.

Use:

```text
game1_playfield.html
```

for active review.
