# Compact Bracket CSS Reflow Note

The bracket item was made smaller, but the old layout controls remained too wide.

This update retunes the entire bracket around the compact square flag tile:

```text
slot
match band
connector
round column
final area
```

The purpose is to keep all visual controls tied to the new smaller item.
