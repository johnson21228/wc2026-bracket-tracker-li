# Prompt — Replace Spacer Bracket With Source Pods

```text
Update the WC2026 Bracket Tracker static HTML to replace the current spacer-based bracket layout with a source-adjacent pod layout.

Read LI first.

Requirements:
- Use nested CSS grid or flex pods.
- Place each R16 result directly adjacent to the two R32 matches that feed it.
- Compose two R16 pods into a QF pod.
- Compose two QF pods into an SF pod.
- Compose semifinal pods into the Final.
- Preserve drag/drop team behavior.
- Preserve export/import JSON state.
- Preserve slot labels and tooltips.
- Preserve screenshot readability.
- Produce a new static HTML release.
- Produce WB_SESSION Capture Back.
```
