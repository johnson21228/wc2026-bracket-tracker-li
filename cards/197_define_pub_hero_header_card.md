# Card 197 — Define pub hero header

## Intent

Replace implementation-facing hero copy with pub/game-facing product language and add visible pick import/export utilities.

## Acceptance

- Hero eyebrow is `Footie FÜHN`.
- Hero title is `Bracketeering Pub`.
- Hero subtitle is `Scroll the game board below and make your picks.`.
- Header actions include `Clear picks`, `Export picks`, and `Import picks`.
- Export creates a JSON pick snapshot.
- Import accepts a JSON pick snapshot through model validation.
- The app remains one deployable site under `site/`.
