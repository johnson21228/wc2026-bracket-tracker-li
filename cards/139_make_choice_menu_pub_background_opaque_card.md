# Card 139 — Make choice menu pub background opaque

## Intent
Use the same dark pub-wood color family as the rendered pick cards for choice menus, but make the menu background fully opaque so long candidate menus remain legible during review.

## Acceptance
- Choice/tap menus use `rgb(26, 17, 10)` as an opaque background.
- Menu content and menu items remain fully opaque.
- Existing pick card styling remains unchanged.
- Verifiers pass.
