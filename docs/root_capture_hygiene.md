# Root Capture Hygiene

Workbench repo root should stay clean enough to act as the entry surface for humans and inference interfaces.

Capture Back records are durable evidence, but they should not accumulate at root.

Use:

    captures/

for:

    CAPTURE_BACK_*.md

Root remains focused on canonical entry files, governance files, and automation files.
