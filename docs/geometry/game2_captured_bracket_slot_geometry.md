# Game 2 Captured Bracket Slot Geometry

The shared middle-layer PNG currently defines visible bracket geometry. The file `site/data/game2_bracket_geometry_slots.json` captures that geometry as named slots.

The JSON is not yet the source that generates the image; it is the measured model used by runtime logic to place bracket items consistently into the current image-defined board.
