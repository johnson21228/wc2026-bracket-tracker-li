# WC2026 copyable developer properties

The developer frame exposes a copyable JSON snapshot of the current board presentation state.

This supports a visual tuning loop:

1. Adjust controls in the browser.
2. Copy the live properties snapshot.
3. Paste the snapshot into the workbench chat.
4. Promote selected values to durable defaults.
