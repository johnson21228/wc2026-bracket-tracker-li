# WC2026 pick identifier developer overlay

The pick identifier overlay renders model/geometry identifiers inside their visual pick slots.

It is used to verify that the native pixel board, manifest slot definitions, and future pick model agree about slot identity.
