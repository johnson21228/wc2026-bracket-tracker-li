# Opaque menu pub background

The review menu background should visually match the rendered pick card family, but unlike the picks it should be fully opaque. This prevents the bracket board or flag layer from showing through long candidate menus, especially the third-place candidate menu.

Canonical color: `rgb(26, 17, 10)`.
