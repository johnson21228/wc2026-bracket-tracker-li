# Identity Round Icon Buttons

Join-first identity controls use the same round icon-button visual language as the map controls.

- Join is represented by a person-add SVG icon.
- Profile is represented by a person-check SVG icon.
- Buttons remain accessible through `aria-label` and native `title` tooltips.
- This is presentation-only and does not change auth, profile, storage, or join behavior.
