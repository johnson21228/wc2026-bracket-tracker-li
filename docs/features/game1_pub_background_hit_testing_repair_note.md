# Game 1 Pub Background + Hit Testing Repair

Game 1 uses the pub JPEG as a back layer. Because the board template image can be visually opaque, the page now exposes opacity controls for the pub image, board image, and wash layer. A debug hit-zone toggle makes it easy to confirm that all 32 hotspots are active.
