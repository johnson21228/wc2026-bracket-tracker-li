# Group Stage hides later-round pick fills

During Group Stage presentation, the board renders Round of 32 pick fills but suppresses filled team rendering for later rounds.

This is a presentation rule only:

- stored picks are preserved
- pick write/read state is unchanged
- later-round fills return when Knockout Stage presentation is active
- Game 2 resolved R32 display remains visible
