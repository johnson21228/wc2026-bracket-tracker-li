# Game 1 Pub Background Image Note

Game 1 now supports a static pub background image layer behind the board.

The selected image is stored at:

```text
site/assets/playfield/game1_pub_options_background.jpeg
```

Opacity is controlled by:

```css
--pub-game1-bg-opacity
```

The layer is decorative and non-interactive.
