# Layered Playfield Bracket-Only Note

Use layers:

```text
team flag background
gradient wash
bracket graphics only
hotspots + picked flags + UI text
```

This keeps the bracket geometry separate from branding and later UI content.
