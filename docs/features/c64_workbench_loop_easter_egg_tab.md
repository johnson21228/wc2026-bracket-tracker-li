# C64 Workbench Loop Easter Egg Tab

The Workbench Easter egg panel includes a `C64 Loop` tab.

This tab presents the C64 Workbench Stage 0 Learning Loop image and copy explaining how conversation, local repo changes, terminal verification, cc65 builds, VICE emulator evidence, and Capture Back form a repeatable learning loop.
