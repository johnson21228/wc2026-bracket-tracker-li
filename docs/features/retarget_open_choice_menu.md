# Retarget Open Choice Menu

The bracket board should support fast movement between picks. If a menu is open and the user taps a different pickable slot, the same tap should transfer the menu to that new slot.

This preserves the board-attached menu model while avoiding the two-tap pattern where the first tap closes and the second tap opens.
