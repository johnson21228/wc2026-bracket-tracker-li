# Player Standings Score Headers

Player Standings column labels should match the scoring model:

- `Score` for earned points.
- `Max Possible` for the player's current points ceiling.

This is a presentation alignment step before full runtime scoring implementation.
