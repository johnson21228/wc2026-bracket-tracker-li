# Pixelated Runtime Flag Background Rule

The bottom board layer should use a team flag as visual texture, not crisp content.

Render it large, blocky, low-resolution, pixelated, significantly transparent, and beneath the readability wash and bracket board.

Default background team is USA.

When the first pick is entered, the background may automatically adopt that selected team unless the player has manually chosen a background. Manual selection locks the override.
