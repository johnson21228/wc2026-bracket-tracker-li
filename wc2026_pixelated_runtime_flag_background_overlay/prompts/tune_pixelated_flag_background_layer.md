# Prompt — Tune Pixelated Flag Background Layer

Adjust the runtime flag background layer.

Keep team-data-driven background, USA default, first-pick auto adoption unless manually locked, pixelated rendering, low opacity, and board readability.
