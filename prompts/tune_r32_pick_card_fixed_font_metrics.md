# Tune R32 Pick Card Fixed Font Metrics

Use this prompt to continue refining R32 pick-card metrics.

Preserve the invariant that the board slot rectangle is the authority. The card must fit inside the slot. The flag should be as large as the slot height permits, and all team names should use the same font metrics chosen against the complete 48-team set.

Do not solve long team names by allowing cards to grow beyond `boundsPx`. Prefer two-line wrapping and approved display breaks over per-team font-size changes.
