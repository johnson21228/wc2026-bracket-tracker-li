# Prompt — Repair Game 1 knockout choice menu runtime wiring

Repair Game 1 so R16, QF, and SF slots open a winner choice menu from the two contestants feeding the tapped slot. Preserve R32 group-eligibility menu behavior. Use the command-line knockout choice resolution tests as evidence that contestant resolution works outside the browser, then wire the runtime click/menu path to the same resolution semantics.
