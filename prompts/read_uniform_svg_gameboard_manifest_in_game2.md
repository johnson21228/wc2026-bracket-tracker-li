# Prompt — Read Uniform SVG Gameboard Manifest in Game 2

Update Game 2 so it can read and validate the uniform SVG gameboard manifest without switching the visible board or changing runtime bracket-card behavior.

Preserve Game 1. Preserve Game 2 visual behavior. Add only a read-only probe and LI evidence.
