# Rebuild Game 1 Assignment Layer From Shared Board

Use the shared 1536 × 1024 pixel-native board as the visual and geometric foundation for Game 1.

Add Game 1 behavior only as an overlay layer:

- 32 R32 native-pixel slot targets
- team chooser
- localStorage pick state
- exportable JSON

Do not change Game 2 behavior. Do not introduce a second board image. Do not position Game 1 slots by viewport or page percentages.
