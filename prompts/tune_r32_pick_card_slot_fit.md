# Tune R32 Pick Card Slot Fit

Patch the WC2026 bracket tracker so filled Round of 32 pick cards fit inside their pixel-defined slot bounds.

Preserve the board geometry as authority. Do not expand visible pick cards beyond `boundsPx`. Make flags as large as the slot height supports, fit team names into the remaining width, and move slot/rule metadata to tooltip/details surfaces.
