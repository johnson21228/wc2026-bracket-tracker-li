# Capture Game 2 Bracket Slot Geometry

Using the current Game 2 bracket geometry image as visual source, create a percent-based bracket slot JSON file. Each slot should include slotId, round, x, y, w, h, sourceSeedId where applicable, and feedsTo where known.

The resulting runtime should render bracket items into these slots instead of rendering a separate team list.
