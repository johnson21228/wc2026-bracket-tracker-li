# Implement Game 2 Bracket Item Slot Fit

Use the existing shared middle-layer PNG as the current visual geometry source. Capture the R32 image-defined slots into percent-based geometry data and update Game 2 so seeded teams become bracket items rendered inside those slots.

Do not touch Game 1. Preserve the shared middle-layer board. Do not render seeded teams as a loose list.
