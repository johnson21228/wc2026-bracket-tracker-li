# Repair Uniform SVG Gameboard Manifest Authority

Repair the WC2026 uniform SVG gameboard authority layer so the SVG is the geometry truth, the manifest is derived from SVG rectangles, and the verifier checks the current 61-card board model rather than generic 63-slot tournament math.
