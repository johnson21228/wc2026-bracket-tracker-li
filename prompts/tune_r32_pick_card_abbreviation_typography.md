# Tune R32 Pick Card Abbreviation Typography

Inspect the Game 1 R32 pick card rendering. Preserve the rule that the compact card face renders only flag + three-letter team abbreviation. Adjust CSS and rendering helpers so all cards use the same abbreviation font metrics, the flag is as large as the slot height allows, and the card contents do not clip inside the board-defined slot bounds.
