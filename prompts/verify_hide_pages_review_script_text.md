# Verify hide Pages review script text

Check that the Pages review pick acceptance block in `site/index.html` is wrapped in a `<script>` element and no longer renders as visible text below the game board. Run the targeted verifier and the main WC2026 verifier.
