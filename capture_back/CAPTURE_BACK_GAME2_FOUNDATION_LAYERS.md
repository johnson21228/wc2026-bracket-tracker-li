# Capture Back — Game 2 Foundation Layers

Game 2 has been reset to a foundation surface rather than a premature champion picker.

The page now matches Game 1's visual architecture for the first two layers:

- pub photo back layer,
- transparent bracket geometry PNG middle layer.

Picking behavior, eligible-team rules, and the Game 2 objective remain intentionally deferred until the next rule capture.
