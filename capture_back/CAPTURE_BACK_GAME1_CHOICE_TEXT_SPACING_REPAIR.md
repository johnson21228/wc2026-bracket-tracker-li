# Capture Back — Game 1 Choice Text Spacing Repair

This Capture Back records a player-facing readability repair for Game 1 chooser options.

The issue: chooser choices could render visually as compressed text, for example `MexicoMEX · Group A`.

The fix: choice rows now separate team name and metadata with explicit markup and CSS gap while preserving R32 hit/rule integrity.
