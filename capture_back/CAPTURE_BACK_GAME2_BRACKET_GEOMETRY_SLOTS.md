# Capture Back — Game 2 Bracket Geometry Slots

This CB captures the current PNG-defined Game 2 bracket geometry as data and updates Game 2 so seeded R32 bracket items render into those captured slots.

Current stage: the shared middle-layer PNG defines visible geometry.
Next stage: this JSON can be tuned against the PNG.
Future stage: the JSON geometry can become truth and generate PNG/SVG.
