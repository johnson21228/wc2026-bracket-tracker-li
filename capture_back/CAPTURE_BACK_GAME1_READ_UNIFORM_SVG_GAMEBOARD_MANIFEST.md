# Capture Back — Game 1 Reads Uniform SVG Gameboard Manifest

Game 1 can now load the uniform SVG gameboard manifest as a non-visible migration probe. This confirms that the app can consume the SVG-derived geometry contract before any visible board switch.

The existing visible board remains unchanged. The JSON manifest remains the contract; the generated JavaScript shim is only for reliable static/local review.
