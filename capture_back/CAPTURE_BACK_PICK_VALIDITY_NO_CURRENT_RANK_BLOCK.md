# Capture Back: Pick validity no current-rank block

Card 208 records a pick-validity boundary: current group standings rank is context, not a structural conflict.

The runtime should still limit menus by the bracket slot's source scope. Within that source scope, a user may pick any offered team. If that pick disagrees with the current standings rank implied by the slot label, the app should not mark it red solely for that reason.

The invalid warning remains for conflicts like duplicate Round-of-32 assignments or teams outside the source scope.
