# Capture Back — Game 1 Transparent SVG Middle Layer

This capture repairs the Game 1 board migration so the uniform SVG is not treated as an opaque board picture. The SVG is now transparent linework and slot outlines in the middle layer. The bottom pub/background layer remains visible through it.

The same SVG-derived manifest now drives Game 1 R32 hit-target and rendered pick-card geometry. Game 1 slot qualification/menu rules continue to come from the Game 1 data bundle. Game 2 remains unmigrated.
