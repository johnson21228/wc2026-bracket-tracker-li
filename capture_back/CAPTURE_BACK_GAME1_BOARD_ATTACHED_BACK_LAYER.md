# Capture Back — Game 1 Board-Attached Back Layer

Game 1 was given an explicit board-attached back layer behind the shared transparent pixel-native game board.

This keeps the visual layer stack aligned with the shared board geometry while preserving the PNG as geometry truth.
