# Capture Back — Group Button Rail Visual Anchor

This capture adds a visual and placement refinement to the group-button rail.

The group rail now has explicit LI requiring subtle, partially translucent group tiles at rest and fully opaque active states on hover/focus/touch-over. The shared group panel is opened over or above the group button or group label that launched it, with clipping avoidance against the visible gameboard viewport.

The panel remains on the scrollable gameboard plane and can scroll internally when content is too tall.

No pick logic, third-place semantics, standings data, match data, or runtime scraping behavior is changed.
