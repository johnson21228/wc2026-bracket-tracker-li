# Capture Back — R32 Pick Card Rendering and Tooltips

This capture back adds a shared LI boundary for filled Round of 32 pick cards.

The visual rule is that the card reads as a selected team first. Extended slot/rule explanation moves into a tooltip/details surface.

The first code patch applies this to Game 1 filled pick cards.
