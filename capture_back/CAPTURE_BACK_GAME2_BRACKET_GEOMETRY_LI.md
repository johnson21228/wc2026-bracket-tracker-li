# Capture Back — Game 2 Bracket Geometry LI

This CB captures the Workbench rule that Game 2's bracket geometry is both a presentation surface and a logical topology.

The current transparent bracket geometry image is treated as the visual source of truth. The next implementation should capture matching bracket-slot data from that image. Future implementations should generate the image/SVG from the same slot data used by runtime logic.
