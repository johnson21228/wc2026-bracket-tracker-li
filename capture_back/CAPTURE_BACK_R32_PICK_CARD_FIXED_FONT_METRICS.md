# Capture Back — R32 Pick Card Fixed Font Metrics

This capture adds LI and Game 1 UI tuning so R32 pick cards use a single shared metric system chosen against the full 48-team display-name set.

The card remains inside the board-defined slot. The flag uses the available vertical space. The team name uses fixed metrics and wraps to two lines when necessary.
