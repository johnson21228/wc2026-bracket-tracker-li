# Capture Back — R32 PNG Geometry Source

The current R32 bracket PNG is now captured as the geometry authority for the WC2026 bracket games.

The PNG defines the visible middle-layer geometry. Slot geometry JSON must be measured from this PNG rather than guessed independently.

The future transition to numeric truth geometry is preserved, but generated middle-layer assets are not authoritative until explicitly promoted.
