# Capture Back — Middle Layer Image Geometry Source

This Capture Back records the board-geometry source rule:

- The shared middle-layer bracket PNG currently defines bracket geometry.
- Slot geometry data should be captured from that PNG.
- Bracket items and hit targets should align to captured slots.
- Future generated geometry should reverse the source direction so truth geometry data generates the visible PNG/SVG.

No runtime files are changed.
