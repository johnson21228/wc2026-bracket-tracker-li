# Capture Back — Game 1 QF/SF Winner Picks

This capture-back extends the long-lived Game 1 bracket workspace so QF and SF slots can be picked from resolved prior-round winners.

It preserves the current R32 assignment model and the first-order R16 winner-pick layer. It adds QF/SF advancement picks as the next bracket-lifecycle layer.
