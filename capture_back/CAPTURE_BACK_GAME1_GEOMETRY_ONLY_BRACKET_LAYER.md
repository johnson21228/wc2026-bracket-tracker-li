# Capture Back — Game 1 Geometry-Only Bracket Layer

Created a clean alpha-channel bracket geometry layer for Game 1. The pub JPEG remains the atmosphere/background. The middle layer now contains only bracket geometry: empty slot boxes, connector lines, and the outer frame. Slot numbers are removed from the visible board art because slot identity belongs to data and runtime interaction.
