# Capture Back — Game 1 Uniform SVG Board Layer

Game 1 now uses the uniform SVG gameboard asset as its visible board layer.

This is a visual board-layer migration only. The existing background layer, pick-card layer, hit-target layer, chooser, menu, and tooltip behavior remain in place.

The uniform manifest read-only probe remains in Game 1 so the page can validate the 61-slot uniform board model, but Game 1 placement is still not manifest-driven.

Game 2 is intentionally not switched in this Capture Back.
