# Capture Back — Force Visible Game 1 Hit Targets

Game 1 now uses an explicit #hitLayer above the pub image and transparent bracket overlay. Hotspot buttons are semi-opaque by default and remain pointer-events:auto.
