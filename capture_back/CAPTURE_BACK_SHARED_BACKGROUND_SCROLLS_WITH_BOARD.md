# Capture Back — Shared Background Scrolls With Board

The shared pub/background layer was corrected so it scrolls with the pixel-native board plane instead of remaining fixed to the browser viewport.

This preserves the layer relationship: background, board PNG, hit targets, and rendered bracket items move together.
