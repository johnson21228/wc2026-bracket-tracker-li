# Capture Back — Game 1 Pub Background Image

This capture adds the generated pub background image to the deployable Game 1 site surface.

The background communicates the origin story of the Workbench game: a pub photo, a match board, many choices, and a playful bracket game surface.

Implementation requirements:

- Background image must sit behind the board template.
- Background image must use `pointer-events: none`.
- Opacity must be controlled by a CSS custom property.
- The page should expose a range control for tuning opacity without code changes.
