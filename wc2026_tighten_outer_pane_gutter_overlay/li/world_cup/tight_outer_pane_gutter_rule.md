# Tight Outer Pane Gutter Rule

## Purpose

The gap between the left team pane and bracket pane should match the small spacing used elsewhere in the site.

## Rule

Use a small pane gutter, currently:

```css
--pane-gutter: 8px;
```

The left team pane and bracket pane should appear adjacent, not separated by a large black strip.
