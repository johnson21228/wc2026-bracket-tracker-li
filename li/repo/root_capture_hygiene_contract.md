# Root Capture Hygiene Contract

The root of a Workbench LI repo is an entry surface.

Do not allow root to become an evidence dump.

Capture Back records must be stored under:

    captures/

rather than left at root.

This keeps Workbench repos readable, skimmable, and easier to re-enter from a fresh II session.
