# Game 1 Runtime Duplicate State and Slot Fill Fix Rule

Game 1 must have exactly one top-level `const STORAGE_KEY` declaration. Duplicate state declarations are invalid because they stop browser JavaScript parsing and prevent hit target creation. The visible slot fill control token is `slotFillOpacity`.
