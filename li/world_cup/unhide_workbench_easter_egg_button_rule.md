# Rule — Workbench Easter Egg Button Visible

The Workbench/Easter egg floating button must remain visible unless a future card explicitly hides it again.

Required:
- `data-workflow-panel-open` entry button is present
- entry button is not hidden
- entry button is not `aria-hidden="true"`
- entry button is not removed from keyboard tab order
- Workbench panel code remains wired
