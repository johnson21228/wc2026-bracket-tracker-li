# Group Panel Anchor Rule

A group panel launched from a group rail button should open over or immediately above that launching button when space allows. The panel must be clamped to the visible board viewport and scroll internally when needed. DOM placement state belongs to the View, not the Controller.
