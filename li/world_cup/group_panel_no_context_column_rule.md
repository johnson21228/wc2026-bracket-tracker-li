# Rule — Group panel omits player-facing Context column

The Bracketeering group panel should keep the player-facing standings table focused on rank, team, match stats, goal stats, and points.

Do not render a `Context` column or qualification badges inside the standings table. Qualification context may remain in model/data for internal logic, but it should not add player-facing table noise unless a future card intentionally restores it in a different presentation.
