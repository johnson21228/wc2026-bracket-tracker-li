# Pick identifier developer overlay rule

The developer surface may show pick identifiers inside pick slots for inspection.

Requirements:

- Identifiers must be read from the geometry manifest.
- Labels must be shown inside the native pixel slot bounds.
- The overlay must be toggleable.
- The overlay must not become player-facing game state.
