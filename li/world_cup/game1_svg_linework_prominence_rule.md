# Game 1 SVG Linework Prominence Rule

The uniform SVG remains the transparent middle gameboard layer and geometry authority projection.

After visual review, the bracket connector and slot-outline color must be moved halfway from the darker sampled brown toward white so the full bracket structure reads more clearly over the bottom image layer.

Presentation values:

- pick-card slot fill: `#816A51`;
- bracket connector and slot outline stroke: `#AA9691`;
- `#AA9691` is the midpoint blend from the prior sampled dark linework `#542C23` toward white `#FFFFFF`;
- R32 selectable slots retain stronger fill opacity than later-round slots so Game 1 communicates selectable targets.

The SVG background remains transparent. The bottom/pub layer must remain visible through the board. Pick cards, hit regions, menus, and tooltips remain above the SVG layer.

Game 1 may use this presentation. Game 2 must not be migrated by this change.
