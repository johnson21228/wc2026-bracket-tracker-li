# Gameboard visual defaults rule

Gameboard outline presentation defaults may be promoted from copied developer properties when the browser-tuned values are pasted back into the workbench.

Promoted values are presentation defaults only. They must not alter:

- source SVG authority
- geometry manifest authority
- player pick state
- bracket model state
