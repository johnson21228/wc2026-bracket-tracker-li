# Game 1 Pub Background Hit Testing Rule

Decorative visual layers must not intercept pointer events. Game 1 hit targets must remain above the pub background, wash, and board template. When adding or tuning background art, keep a visible hit-zone debug mode available until the playfield is stable.
