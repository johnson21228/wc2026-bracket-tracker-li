# Apply Selection to Picked Cell Rule

A knockout choice menu selection must be applied to the bracket cell that opened the menu.

The active cell is the write target. The selected team must be stored under that cell's slot id, the stored pick must carry assignment metadata, and the bracket must be rendered immediately after the write.

A visible menu choice that does not update the opening bracket cell is a failed interaction.
