# Identity Round Icon Buttons Rule

Identity actions must read as browser/map chrome, not text buttons.

The Join action is a round person-add SVG icon button.
The Profile action is a round person-check SVG icon button.
Both controls must retain accessible labels and tooltips.
This rule is visual-only and must not change join-first auth/profile behavior.
