# On-page developer console rule

The clean WC2026 site must expose startup/runtime diagnostics on-page during developer work.

A board startup failure must not leave a silent black page. The app entrypoint must render visible diagnostic output when startup fails.
