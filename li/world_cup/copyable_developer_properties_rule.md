# Copyable developer properties rule

During visual tuning, the developer frame may expose a copyable snapshot of current presentation properties.

The snapshot is evidence for choosing defaults. It must not become player state and must not mutate source assets.
