# Rule: Player Standings Score Headers

Player Standings must use scoring-oriented column headers.

Rules:
- The earned-points column is labeled `Score`.
- The maximum-points-ceiling column is labeled `Max Possible`.
- The old `Group` and `Knockout` headers must not be rendered as standings scoring headers.
