# Game 1 Menu Flag And Transparency Rule

Game 1 group-card tap menus should make country identity visually immediate. Team rows must use larger flag glyphs and transparent item surfaces rather than opaque blocks, so the menu reads as a layer over the board/back layer without hiding the game-board context.
